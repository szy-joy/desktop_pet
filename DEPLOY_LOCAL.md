# 本地部署桌面软件指南 (Electron)

我已经将你的项目升级为支持 **Electron** 的桌面应用架构。你可以通过以下步骤将其安装到你的电脑上。

## 1. 准备工作
-   **安装 Node.js**: 前往 [nodejs.org](https://nodejs.org/) 下载并安装长期支持版 (LTS)。
-   **下载项目**: 在 AI Studio 右上角的菜单中选择 **Export** -> **Download ZIP**。

## 2. 运行项目
1.  解压下载的 ZIP 文件。
2.  在文件夹路径中打开终端（Cmd 或 PowerShell）。
3.  运行命令安装依赖：
    ```bash
    npm install
    ```
4.  运行本地开发预览版：
    ```bash
    npm run electron:dev
    ```

## 3. 生成 EXE 安装包
如果你想生成一个给别人安装的 `.exe` 文件：
1.  在终端运行打包命令：
    ```bash
    npm run electron:build
    ```
2.  打包完成后，你可以在新生成的 `release` 文件夹中找到生成的 `.exe` 文件（便携版或安装程序）。

## 已实现的桌面特性
-   **背景透明**: 只有猫猫和气泡显示在桌面上。
-   **窗口置顶**: 猫猫会一直待在其他窗口上方。
-   **自由拖动**: 你可以直接在桌面上拖拽猫猫。
-   **右键菜单**: 右键点击猫猫即可调出设置面板。

祝你和你的猫猫玩得开心！
